#include<stdio.h>
int main(int argc, char const *argv[])
{
    int a=12;
    printf("\n the original number is %d",a);
    printf("\n the number is shifted one bit to the right is %d",a>>1);
     printf("\n the number shifted two bits to th right is %d",a>>2);

    return 0;
}
